# Python
__pycache__/
*.py[cod]
*.pyo
venv/
*.egg-info/
dist/
*.egg

# Node
node_modules/
frontend/dist/
*.tsbuildinfo

# 日志相关文件/文件夹（不提交）
dump_info.tar.gz
dump_info/

# 上传临时目录
backend/uploads/*
!backend/uploads/.gitkeep

# 数据目录（运行时生成）
backend/data/datasets/*.json
backend/data/sessions/*.json
backend/data/*.db
backend/data/source/
# 运行时数据备份（如 _backup_YYYYMMDD_HHMMSS/），含 db/json，勿入库
backend/data/_backup*/
# 评测运行产物（每次 run 的逐条明细 + 记分卡）——过程记录，勿入库
backend/data/eval_runs/

# 过程记录文件（个人笔记/草稿）——正式记录只入库 result/ + CHANGELOG.md + VERSION
DEV_NOTES.md
docs/claude-code-agent-loop.md

# IDE & OS
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/

# 环境变量
.env
.env.local
.env.*.local
